.fixed-menu {
	position:fixed;
	left:0;
	top:0;
	width:100%;
	#fe.bg(#ffffff);
	
	.menu-item {
		
		&.active {
			#fe.bg(#1b9970);
			
			a {
				#fe.color(#ffffff);
			}
		}
		
		a {
			display:block;
			text-align:center;
			cursor:pointer;
			margin:0em auto;
			padding:2em 0;
			#fe.color(#1b9970);
			#fe.bold();
			#fe.uppercase();
		}
	}
	
}